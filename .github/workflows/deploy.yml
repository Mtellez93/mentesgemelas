name: Deploy Mentes Gemelas

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 5.189.170.159
          username: miguel
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /var/www/mentesgemelas

            echo "📥 Pull latest changes..."
            git pull origin main

            echo "📦 Installing dependencies..."
            npm install

            echo "♻️ Restarting app..."
            pm2 restart mentes-gemelas || pm2 start index.js --name mentes-gemelas

            pm2 save
